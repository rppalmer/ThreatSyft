# Upper bounds are deliberate. For mcp the bound is load-bearing: 2.0.0 removes
# mcp.server.fastmcp entirely (verified against 2.0.0 in a clean venv) and both
# servers are built on it, so crossing this pin is a rewrite rather than an
# upgrade. Its replacement appears to be mcp.server.mcpserver.
#
# The [cli] extra is intentionally absent. It pulls typer, rich, uvicorn,
# starlette, sse-starlette, python-multipart, PyJWT and cryptography to support
# the `mcp` command-line tool, none of which these stdio servers use.
mcp>=1.28,<2

certifi>=2026.6.17
dnspython>=2.8,<3
httpx>=0.28,<0.29
ipwhois>=1.3,<2
pydantic>=2.13,<3
python-dotenv>=1.2,<2
python-whois>=0.9.6,<0.10
